[pytest]
testpaths = tests
norecursedirs = .git .venv node_modules frontend HoSo_BaoCao_ParkingAI
addopts = --strict-markers
# A hung test dumps every thread's stack to stderr after 10 minutes so a
# cancelled CI job leaves evidence instead of a bare progress line.
faulthandler_timeout = 600
