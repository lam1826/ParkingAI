// Replaced by the deployment workflow after downloading the immutable build.
// Empty means same-origin; local development still falls back to port 8000.
globalThis.__PARKINGAI_CONFIG__ = Object.freeze({ API_URL: "" });
